[[source]]
url = "https://pypi.python.org/simple"
verify_ssl = true
name = "pypi"

[requires]
python_version = "3.9"

[packages]
boto3 = "~=1.34"
cryptography = "~=42.0"
cwe = "~=1.6"
django = "~=3.2"
graphql-query = "~=1.3"
packaging = "==21.3"  # Must match version in Makefile.
psycopg2-binary = "~=2.9"
PyGithub = "~=2.1"
pyjwt = "~=2.8"
python-jose = "~=3.3"
requests = "~=2.31"
simplejson = "~=3.19"
sqlparse = "~=0.5"
awscli = "~=1.32"

[dev-packages]
aiohttp = "~=3.9"
autopep8 = "~=2.1"
black = "==22.3.0"  # Pending upgrade later.
cfn-lint = "==0.53.0"  # Must match version in Makefile.
checkov = "==2.0.1065"  # Must match version in Makefile.
flake8 = "~=7.0"
gitpython = "~=3.1"
isort = "~=5.12"
mock = "~=5.1"
moto = {extras = ["awslambda", "sqs", "s3", "ec2", "secretsmanager"], version = "==5.0.8"}  # Locked to avoid resolving to prerelease versions.
pylint = "~=3.0"
pytest = "~=8.2"
pytest-cov = "~=5.0"
pytest-subtests = "~=0.12"
python-dotenv = "~=1.0"

[pipenv]
allow_prereleases = true
