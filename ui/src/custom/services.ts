import { ServiceMapT } from "app/services";

// private (internal) VCS services, use url field for hostnames
export const customServiceMap: ServiceMapT = new Map([
	// REPLACE ME: POPULATE ANY INTERNAL VERSION CONTROL SYSTEMS HERE
]);
