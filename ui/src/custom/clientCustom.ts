import { AppMeta } from "./scanMetaSchemas";

const adjustMetadata = (appMeta: AppMeta) => {
	// REPLACE ME: ADD ANY CUSTOM LOGIC YOU NEED FOR ADJUSTING application_metadata
	// SCHEMA FIELDS HERE, SUCH AS SORTING DATA OR PERFORMING STRING => DATE CONVERSIONS
};
export default adjustMetadata;
