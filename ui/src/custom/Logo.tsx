// REPLACE ME: IF YOU WANT TO USE A CUSTOM APP LOGO, DEFINE IT HERE, E.G. USING MUI <SvgIcon>
// NOTE: YOU MUST ALSO SET REACT_APP_CUSTOM_LOGO="true" IN YOUR .env FILES
const Logo = (props: any) => {
	// note: logo is purely decorative/branding
	// its omission does not change any meaning in the page
	// so no svg title or aria tag is needed for a11y here
	// by MUI defaults aria-hidden=true will be added to the element if using SvgIcon
	return <></>;
};
export default Logo;
